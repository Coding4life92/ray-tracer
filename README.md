# Building a Ray Tracer

I am following the series Ray Tracing in One Weekend and decided to document my process every step of the way.

## Build

```bash
mkdir build
cmake -B build
cmake --build build
```

## Run

```bash
./build/inOneWeekend > image.ppm
```